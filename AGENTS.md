# Instructions for AI agents

This public repository is a generated publication target for
`clem2000/klzu-aviation-planner`. Read [`README.md`](README.md) first.

Last reconciled with `_core` `main` commit
`020de18894f2f7d60fa464d65bf3bc1123323a2c` (governance revision 0.5).
Current project evidence overrides older `_core` snapshots for project-specific
facts.

## Generated-repository boundary

- Make normal content, behavior, data, rendering, scheduling, and publication
  changes in the private planner repository, then let its guarded publisher
  regenerate this repository.
- Do not hand-edit generated `index.html` or bypass the private planner's
  generation, structural checks, exact-path allowlist, and SHA-256 verification.
  An owner-authorized recovery is the only exception and must follow the
  planner's publication runbook.
- Never add credentials, private endpoints, database access, private rows,
  authorization headers, source-only implementation detail, or other
  non-public information.
- Inspect Git status, the current branch, `main`, open PRs, and the expected
  generated ownership before a change. Preserve unrelated work.
- Keep answer, review, and report-only tasks read-only. Any authorized
  repository change uses a short `codex/` branch and an unmerged pull request
  for owner review; never push or merge directly to `main`.

For instruction-only changes, validate the Markdown links, UTF-8 without BOM,
LF endings, and the exact diff. Do not trigger generation, publication,
deployment, or another production action merely to validate this file.
